<%= form_with(model: page_view, local: true) do |form| %>
  <% if page_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_view.errors.count, "error") %> prohibited this page_view from being saved:</h2>

      <ul>
      <% page_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :referrer %>
    <%= form.text_field :referrer %>
  </div>

  <div class="field">
    <%= form.label :hash %>
    <%= form.text_field :hash %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.datetime_select :created_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
